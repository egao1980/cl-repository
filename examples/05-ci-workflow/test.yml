# Consumer test.yml — canned reusable workflow. No per-repo Lisp scripts.
name: Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    uses: egao1980/cl-repository/.github/workflows/test-system.yml@main
